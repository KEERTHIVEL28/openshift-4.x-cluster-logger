apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: "{{ eo_name }}"
  namespace: "openshift-operators-redhat"
spec:
  channel: "{{ eo_channel }}"
  installPlanApproval: "{{ eo_installPlanApproval }}"
  source: "{{ eo_source }}"
  sourceNamespace: "{{ eo_sourceNamespace }}"
  name: "{{ eo_name }}"
