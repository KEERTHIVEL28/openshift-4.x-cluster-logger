apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: "{{ clo_name }}"
  namespace: openshift-logging 
spec:
  channel: "{{ clo_channel }}" 
  name: "{{ clo_name }}"
  source: "{{ clo_source }}"
  sourceNamespace: "{{ clo_sourceNamespace }}"